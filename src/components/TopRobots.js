import React from 'react';

const TopRobots = ({ searchfield, searchChange }) => {
  return (
    <div className='top-robots'>
        <a href="#" className="chat-link">Top Robots</a>
    </div>
  );
}

export default TopRobots;